global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: employee-agent
    metrics_path: /internal/metrics
    static_configs:
      - targets:
          - 127.0.0.1:5180
        labels:
          service: employee-agent
